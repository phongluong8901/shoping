// tao url local nhưng public tam cho flutter
Chạy FastAPI của bạn ở cổng 8000 trước.

Mở một Terminal mới và gõ:

Bash

npx localtunnel --port 8000
Nó sẽ trả về một cái link dạng: https://xxxx-xxxx-xxxx.loca.lt

Copy link đó dán vào code Flutter là xong.

#----------------------------------------------------------
// dung postgress cho render web + docker
tai postgress tren web
tai DBever lam studio theo doi

pip install psycopg2-binary

#----------------------------------------------------------
//them docker
docker build -t fastapi-app .

// kiem tra loi crash
docker logs fastapi-app-container

#----------------------------------------------------------
#docker 2
# Xóa container cũ
docker rm -f fastapi-app-container

# Build lại với code mới
docker build -t fastapi-app .

# Chạy với quyền truy cập vào mạng của máy chủ (Host)
docker run -d -p 8000:8000 --name fastapi-app-container -e DB_HOST=host.docker.internal fastapi-app

# dugn lai
docker stop fastapi-app-container

#----------------------------------------------------------
// chay url ao ket noi voi flutter
npx localtunnel --port 8000

// thay the localtunnel
npx ngrok config add-authtoken 2zjb7L3klhhR2UhyB11fDlido6a_3G3i3Jj79JnQtWtkxgwSq
npx ngrok http 8000

https://e3b482958355.ngrok-free.app

#----------------------------------------------------------
// day len github
git config --global user.email "phongluong3366@gmail.com"
git config --global user.name "phongluong8901"

# 1. Thêm tất cả các file (quan trọng nhất là dấu chấm)
git add .

# 2. Commit lại (lúc này Git đã biết bạn là ai nên sẽ không lỗi nữa)
git commit -m "Full code for FastAPI and Docker"

# 3. Đẩy lên GitHub
git push -u origin main

#---------------------------------------------------------- nap lai cho github

# 1. Kiểm tra các file đã thay đổi
git status

# 2. Thêm tất cả thay đổi
git add .

# 3. Commit với lời nhắn
git commit -m "Update API with LLM and CORS fix"

# 4. Đẩy lên GitHub
git push origin main